import type { HabitFrequency } from "@myos/core/goal";

/** Habit frequency labels (Sprint 2.12). Presentational only. */
export const CYCLE_FREQ_LABEL: Record<HabitFrequency, string> = {
  daily: "Daily",
  weekly: "Weekly",
  monthly: "Monthly",
};
