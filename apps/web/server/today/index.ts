/**
 * Today server domain (Sprint 2.1). Persistence + service + tRPC router. Pure
 * domain logic (types, planner, selectors, schemas) lives in `@myos/core/today`.
 */
export { todayRouter } from "./router";
export * as todayService from "./service";
